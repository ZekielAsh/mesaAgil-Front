import Ionicons from '@expo/vector-icons/Ionicons';

export default function PeopleIcon({ color = 'black' }: { color: string }) {
  return <Ionicons name="people-outline" size={24} color={color} />;
}
