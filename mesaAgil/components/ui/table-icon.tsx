import MaterialCommunityIcons from '@expo/vector-icons/MaterialCommunityIcons';

export default function TableIcon({ color = 'black' }: { color: string }) {
  return <MaterialCommunityIcons name="table-furniture" size={24} color={color} />;
}
